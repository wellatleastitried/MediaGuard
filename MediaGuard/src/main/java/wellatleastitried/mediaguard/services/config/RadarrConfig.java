package wellatleastitried.mediaguard.services.config;

public class RadarrConfig extends Config {

    public RadarrConfig(String host, int port, String apiKey) {
        super(host, port, apiKey);
    }
}
